---
name: process-inbox
description: Convert unprocessed captures into reviewable Noema objects.
---

# Process inbox

Classify each capture as a task, event, or note. Preserve its source and language. Never invent dates. When placement is uncertain, propose a note tagged `needs-filing`. Return proposals only; Noema applies confirmed objects transactionally.

# Status Check — working tree on `8490829` (session 2026-08-24, afternoon)

- **Date:** 2026-08-24 · Continues STATUS_36c6ca0.md
- **Method:** Source edits + new backend tests + live Playwright verification (Chromium probe for CSS `zoom` semantics; Firefox e2e for note zoom/ink alignment)
- **Verification:** backend 123/123 · frontend 181/181 · tsc clean · `test:browser` suite green · new `03-note-zoom.spec.ts` e2e green (3 consecutive runs)

## ✅ Completed this session

### P1 leftovers (tablet ink) — now done except deferred F1.3
| Item | Verdict | Evidence |
|---|---|---|
| Overlay pinch-zoom in notes (F1.2 remainder) | ✅ | CSS `zoom` on `.integrated-doc-page`, driven imperatively by two-finger pinch (non-passive touch listeners, anchor-corrected scrolling) and ctrl/cmd+wheel; "Reset zoom" item appears in More menu when ≠100%. Chromium probe established measurement rules: rect/scrollHeight are post-zoom → normalized by dividing by zoom; offsetWidth/contentRect pre-zoom → used as-is. Fixed a real bug found by the e2e: gesture effect bound at mount to the loading-state container node — re-binds on `[loading]` |
| Ink insert handle wired (F1.4 remainder) | ✅ | `MarkdownBlock` now renders a hover/focus-revealed "Insert ink" action calling the previously-dead `onInsertInk(block, value, caret)` (splits block at end, inserts ink + continuation block, reorders server-side). Visible on coarse pointers |
| Stroke coordinates stay layout-normalized while zoomed | ✅ | Overlay `getPoint` divides by current zoom (`zoomRef`); verified end-to-end: stroke drawn at 130%+ zoom persists with maxX ≤ unzoomed width |
| PDF annotator gestures (F1.6 remainder) | ✅ | Two-finger pinch + ctrl-wheel drive existing `scale` state (clamped .5–2, ±4% commit threshold to avoid render thrash); ± buttons retained |

### P2/P3/P4/P5/P6 partials closed
| Item | Verdict | Evidence |
|---|---|---|
| Note→PDF v2 core gaps (F3.4) | ✅ | `server/note-pdf.mjs` rewritten: display `$$…$$` math renders as centered equation cards via LaTeX→Unicode converter (greek/operators/frac/sqrt/super-subscripts; ASCII fallback without CJK font), inline `$…$` italicized, ink markers vector-drawn from vault JSON sidecars via `drawSvgPath` (y-flip + scale, stroke colors/widths honored), PNG/JPG assets embedded via `/api/v1/assets/{id}` resolution, HTML comment marker lines filtered. Functional test: export produces parseable multi-feature PDF (backend suite) |
| Recorder recovery path (F4.1 remainder) | ✅ | `DurableRecorder` recovery bar now offers **Recover** (assembles IndexedDB chunks into File → `onFinished`, same pipeline as finalize) alongside Discard; list refreshes after either action |
| Tutor stale-editor refresh (F6.3 remainder) | ✅ | `MixedNoteEditor` exposes React-19 ref handle `{refresh, getActiveBlock}`; vault-page tutor `onApply` calls `refresh()` so inserted answer blocks appear immediately |
| Tutor insert caret targeting (F6.2 remainder) | ✅ | Insert API accepts `afterBlockId`; server reorders new provenance-labeled block directly after the active markdown block (last-edited tracked client-side). Plain notes no longer double-insert: panel applies server-returned canonical content instead of appending locally |
| Parallel-tutor quality profile (F6.4) | ✅ | Math-detected parallel asks use `configuredChain("quality")` + `profile:"quality"` matching single mode |
| Calendar parity F5.5 | ✅ | Multi-day events span cells in Month/Day/Agenda via `coversDate(start,end,date)` overlap check (legacy weekday events now appear on all matching days); Month "+N more" opens a full-day popover (all events/tasks, click-to-edit, Add-event footer, Escape/backdrop close); per-cell quick-add "+" (hover-reveal); Day view click-slot creates a 1h snapped event |
| Debug artifact | ✅ | `probe-debug.mjs` removed |

**New tests:** backend `tutor insert lands after the active block when asked`, `note PDF export embeds math, images, and handwriting strokes`; browser `03-note-zoom.spec.ts` (S7-adjacent regression guard).

## ⚠️ Known flake
`02-handwriting.spec.ts` fails intermittently **on clean HEAD too** (verified via stash) — pre-existing, not from this session's changes. Worth its own investigation.

## Remaining (unchanged scope)
- P1: pen-near auto-ink ergonomics (F1.5 nicety); content-anchored strokes (F1.3) — still deferred by mitigation plan
- P2: optimize UI absent inside MixedNoteEditor (vault page has it); typed-capture opt-out only honored for `source:"file"`
- P3 minor: annotator edit uses POST-upsert not PATCH (works); GFM tables still flattened text rows
- P5 minor: undo toast tied to delete only
- P7 binusmaya/study intelligence — not started
- P8 coding (multi-file staging, kill IPC, toolchain detect, mobile nav) — not started
- P9 design/a11y batches 9a–9f — not started

## Suggested next session order
1. P2 leftovers (editor optimize picker + typed opt-out)
2. P8 coding items (small, high Timmy value)
3. P7 import-first wizard (F7.1–F7.3)
4. P9 batch 9a tokens/a11y-critical

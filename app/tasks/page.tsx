"use client";

import {createId} from "../lib/id";

import {FormEvent, useEffect, useState} from "react";
import {Archive, CalendarBlank, Check, Circle, Flag, Plus, X} from "@phosphor-icons/react";
import {ModuleShell} from "../components/ModuleShell";
import {Task, useAppState} from "../components/AppState";
import Link from "next/link";

const blankTask=():Task=>({id:createId(),title:"",project:"Inbox",due:"",dueAt:new Date().toISOString(),priority:"Medium",completed:false,status:"open"});
const jakartaParts=(value:string)=>Object.fromEntries(new Intl.DateTimeFormat("en-GB",{timeZone:"Asia/Jakarta",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).formatToParts(new Date(value)).map(part=>[part.type,part.value]));
const dateValue=(value?:string|null)=>{if(!value)return "";const part=jakartaParts(value);return `${part.year}-${part.month}-${part.day}`};
const dateTimeValue=(value?:string|null)=>{if(!value)return "";const part=jakartaParts(value);return `${part.year}-${part.month}-${part.day}T${part.hour}:${part.minute}`};
const jakartaIso=(value:string)=>new Date(`${value}:00+07:00`).toISOString();

export default function TasksPage(){
  const {tasks,projects,toggleTask,saveTask,archiveTask}=useAppState();
  const [filter,setFilter]=useState("Today");
  const [draft,setDraft]=useState<Task|null>(null);
  useEffect(()=>{const id=new URLSearchParams(location.search).get("open"),task=tasks.find(item=>item.id===id);if(task){setFilter("Inbox");setDraft({...task})}},[tasks]);
  function submit(event:FormEvent){event.preventDefault();if(!draft?.title.trim())return;saveTask({...draft,title:draft.title.trim()});setDraft(null)}
  const today=dateValue(new Date().toISOString()),matches=(task:Task,label:string)=>label==="Completed"?task.completed&&!task.archived:label==="Someday"?!task.dueAt&&!task.completed&&!task.archived:label==="Upcoming"?!!task.dueAt&&dateValue(task.dueAt)>today&&!task.completed&&!task.archived:label==="Overdue"?!!task.dueAt&&dateValue(task.dueAt)<today&&!task.completed&&!task.archived:label==="Today"?dateValue(task.dueAt)===today&&!task.completed&&!task.archived:!task.dueAt&&!task.completed&&!task.archived,counts=Object.fromEntries(["Inbox","Today","Upcoming","Overdue","Someday","Completed"].map(label=>[label,tasks.filter(task=>matches(task,label)).length])),visible=tasks.filter(task=>matches(task,filter)),ready=tasks.filter(task=>!task.completed&&!task.archived).length;
  return <ModuleShell active="Tasks" title="Tasks" action={<button className="primary top-primary" onClick={()=>setDraft(blankTask())}><Plus/>New task</button>}>
    <div className="module-header"><div><h2>What needs doing</h2><p>{ready} task{ready===1?" is":"s are"} ready.</p></div><label className="task-view-select"><CalendarBlank/><span className="sr-only">Task view</span><select aria-label="Task view" value={filter} onChange={event=>setFilter(event.target.value)}>{["Inbox","Today","Upcoming","Overdue","Someday","Completed"].map(label=><option key={label}>{label}</option>)}</select></label></div>
    <div className="task-layout">
      <nav className="subnav" aria-label="Task views">{["Inbox","Today","Upcoming","Overdue","Someday","Completed"].map(label=><button className={label===filter?"active":""} key={label} onClick={()=>setFilter(label)}><span>{label}</span><small>{counts[label]}</small></button>)}</nav>
      <section className="task-list" aria-labelledby="task-list-title"><div className="list-title"><h3 id="task-list-title">{filter}</h3><span>{visible.length} shown</span></div>{visible.map(task=><article className={`${task.completed?"completed ":""}${draft?.id===task.id?"selected":""}`} key={task.id}><button className="task-check" aria-label={`${task.completed?"Reopen":"Complete"} ${task.title}`} aria-pressed={task.completed} onClick={()=>toggleTask(task.id)}>{task.completed?<Check weight="bold"/>:<Circle/>}</button><button className="task-copy" onClick={()=>setDraft({...task})}><strong>{task.title}</strong><span><Flag/> {task.project}{task.subtasks?.length?` · ${task.subtasks.length} subtasks`:""}</span></button><time>{task.due}</time><span className={`priority ${task.priority.toLowerCase()}`}>{task.priority}</span><button className="row-menu" aria-label={`Edit ${task.title}`} onClick={()=>setDraft({...task})}>Edit</button></article>)}{!visible.length&&<div className="empty-state"><Check/><h3>Nothing here</h3><p>This view is clear.</p></div>}</section>
      {draft?<aside className="object-inspector"><div className="object-inspector-head"><div><span>{tasks.some(task=>task.id===draft.id)?"Edit task":"New task"}</span><small>{draft.vaultSource?"Changes rewrite only this Obsidian checklist line.":"Use plain language; changes appear on Today immediately."}</small></div><button className="icon-button" aria-label="Close task inspector" onClick={()=>setDraft(null)}><X/></button></div><form onSubmit={submit}><label>Task name<input autoFocus value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})} placeholder="What needs doing?" required/></label><label>Project<select value={draft.projectId||""} onChange={e=>{const project=projects.find(item=>item.id===e.target.value);setDraft({...draft,projectId:project?.id||null,project:project?.name||"Inbox"})}}><option value="">Inbox</option>{projects.filter(item=>item.status!=="Archived").map(project=><option key={project.id} value={project.id}>{project.name}</option>)}</select></label><div className="field-row"><label>Due<input type="date" value={dateValue(draft.dueAt)} onChange={e=>setDraft({...draft,dueAt:e.target.value?new Date(`${e.target.value}T00:00:00+07:00`).toISOString():null,scheduledStartAt:null})}/></label><label>Priority<select value={draft.priority} onChange={e=>setDraft({...draft,priority:e.target.value as Task["priority"]})}><option>High</option><option>Medium</option><option>Low</option></select></label></div><label>Scheduled time<input type="datetime-local" value={dateTimeValue(draft.scheduledStartAt)} onChange={e=>setDraft({...draft,scheduledStartAt:e.target.value?jakartaIso(e.target.value):null,dueAt:e.target.value?jakartaIso(e.target.value):draft.dueAt})}/></label><label>Reminder<input type="datetime-local" value={dateTimeValue(draft.reminderAt)} onChange={e=>setDraft({...draft,reminderAt:e.target.value?jakartaIso(e.target.value):null})}/></label><label>Repeat<select value={draft.recurrence||"Never"} onChange={e=>setDraft({...draft,recurrence:e.target.value})}><option>Never</option><option>Daily</option><option>Weekdays</option><option>Weekly</option><option>Monthly</option></select></label><label>Subtasks<textarea value={(draft.subtasks||[]).join("\n")} onChange={e=>setDraft({...draft,subtasks:e.target.value.split("\n").filter(Boolean)})} placeholder="One subtask per line"/></label><label className="check-field"><input type="checkbox" checked={draft.completed} onChange={e=>setDraft({...draft,completed:e.target.checked})}/> Mark completed</label>{draft.vaultSource&&<div className="permission-note"><CalendarBlank/><span><strong>{draft.vaultSource.sourceName} · {draft.vaultSource.relativePath}</strong><small>Line {draft.vaultSource.lineNumber} · ^{draft.vaultSource.blockId}</small><Link href={`/vault?open=${encodeURIComponent(draft.vaultSource.noteId)}`}>Open source note</Link></span></div>}<div className="inspector-actions">{tasks.some(task=>task.id===draft.id)&&<button type="button" className="icon-button danger" aria-label="Archive task" onClick={()=>{archiveTask(draft.id);setDraft(null)}}><Archive/></button>}<button type="button" className="secondary" onClick={()=>setDraft(null)}>Cancel</button><button className="primary">Save task</button></div></form></aside>:<aside className="task-focus"><Flag/><h3>Select a task</h3><p>Open any task to change its schedule, recurrence, subtasks, project, or priority.</p><button className="secondary" onClick={()=>setDraft(blankTask())}><Plus/>Add task</button></aside>}
    </div>
  </ModuleShell>
}

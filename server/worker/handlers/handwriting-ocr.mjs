import {readFileSync} from "node:fs";
import {join} from "node:path";
import {runGeminiMultimodal} from "../../ai.mjs";
import {failJob,finishJob} from "../../jobs.mjs";

export async function handleHandwritingOcr({job,config,db}){try{const block=db.prepare("SELECT i.*,s.root_path FROM note_ink_blocks i JOIN note_blocks b ON b.id=i.block_id JOIN vault_entries e ON e.note_id=b.note_id JOIN vault_sources s ON s.id=e.source_id WHERE i.block_id=?").get(job.input.blockId);if(!block)throw new Error("Ink block not found");if(!config.geminiApiKey){db.prepare("UPDATE note_ink_blocks SET ocr_status='unavailable',updated_at=? WHERE block_id=?").run(new Date().toISOString(),block.block_id);finishJob(job.id,{blockId:block.block_id,status:"unavailable"},db);return}const schema={type:"object",required:["text","equations"],properties:{text:{type:"string"},equations:{type:"array",items:{type:"object",required:["latex","confidence"],properties:{latex:{type:"string"},confidence:{enum:["high","medium","low"]}}}}}},base64=readFileSync(join(block.root_path,block.svg_path),"base64"),{result}=await runGeminiMultimodal({prompt:"Extract handwritten text and equations. Strokes remain authoritative.",base64,mimeType:"image/svg+xml",schema,config});db.prepare("UPDATE note_ink_blocks SET transcript=?,equations_json=?,ocr_status='complete',updated_at=?,version=version+1 WHERE block_id=?").run(String(result.text||""),JSON.stringify(result.equations||[]),new Date().toISOString(),block.block_id);finishJob(job.id,{blockId:block.block_id,status:"complete"},db)}catch(error){db.prepare("UPDATE note_ink_blocks SET ocr_status='failed',updated_at=? WHERE block_id=?").run(new Date().toISOString(),job.input.blockId);failJob(job.id,error,db)}}
